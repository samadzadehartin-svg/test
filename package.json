{
  "name": "safaroiranian",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @safaro/web",
    "dev:api": "npm run start:dev --workspace @safaro/api",
    "build:web": "npm run build --workspace @safaro/web",
    "build:api": "npm run build --workspace @safaro/api"
  },
  "engines": {
    "node": ">=20"
  }
}
